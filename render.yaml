# Root Render Blueprint — Web API + Postgres
# In Render: New → Blueprint → connect VinthaRameshReddy/tankermanagerapp

services:
  - type: web
    name: tanker-manager-api
    runtime: docker
    plan: free
    rootDir: backend
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /api/public/health
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: tanker-manager-db
          property: connectionString
      - key: DB_USERNAME
        fromDatabase:
          name: tanker-manager-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: tanker-manager-db
          property: password
      # Public API URL after deploy, e.g. https://tanker-manager-api.onrender.com
      - key: APP_PUBLIC_BASE_URL
        sync: false
      # Free OpenRouteService key: https://openrouteservice.org/dev/#/signup
      - key: MAPS_PROVIDER
        value: openrouteservice
      - key: ORS_API_KEY
        sync: false
      # Fast2SMS — same key names as openplot-api (copy values after Approve)
      - key: FAST2SMS_ENABLED
        value: "true"
      - key: FAST2SMS_API_KEY
        sync: false
      - key: FAST2SMS_SENDER_ID
        value: TANKER
      - key: SMS_PROVIDER
        value: fast2sms
      - key: CORS_ORIGINS
        value: "*"

databases:
  - name: tanker-manager-db
    plan: free
    databaseName: tankermanager
    user: tankermanager
